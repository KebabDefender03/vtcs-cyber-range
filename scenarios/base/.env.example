# ============================================================================
# VTCS Cyber Range - Environment Configuration
# ============================================================================
# Copy this file to .env and customize values
# NEVER commit .env to git (contains secrets)
# ============================================================================

# Database Configuration
DB_NAME=labdb
DB_USER=labuser
DB_PASS=labpass123
DB_ROOT_PASS=rootpass123

# Web Application
WEBAPP_DEBUG=false

# Resource Limits (adjust based on available resources)
# These are already set in docker-compose.yml but can be overridden
# WORKSPACE_CPU_LIMIT=0.4
# WORKSPACE_MEM_LIMIT=2G
